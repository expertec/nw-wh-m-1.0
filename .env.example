# Puerto del servidor
PORT=3001

# Entorno (development | production)
NODE_ENV=development

# Ruta para almacenar sesiones de WhatsApp (auth_info)
# Por defecto: ./auth_info en dev, /var/data en producción
# AUTH_DATA_PATH=./auth_info

# Firebase Service Account (alternativa a archivo)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# Cors (opcional)
# CORS_ORIGIN=http://localhost:3000

# ============== AI AGENT ==============
# API Key de OpenAI (ChatGPT)
OPENAI_API_KEY=sk-...

# Clave maestra de encriptación (64 caracteres hex - generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=12e38d94ff945db2e72a61d64b11f1679b40e577c9f9f6bfb909bec16a54ab9e

# ============== GOOGLE CALENDAR (Fase 4) ==============
# Credenciales OAuth de Google Cloud Console
# GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=xxx
# GOOGLE_REDIRECT_URI=http://localhost:3001/api/integrations/google-calendar/callback
